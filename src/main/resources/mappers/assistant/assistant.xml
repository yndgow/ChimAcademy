<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.ChimAcademy.dao.AssistantDAO">

	<select id="selectDep" resultType="kr.co.ChimAcademy.vo.DepartmentVO">
		SELECT a.*
		FROM department AS a 
		JOIN member AS b 
		USING(depCode) 
		WHERE uid = #{uid};
	</select>
	
	<select id="selectLectures" resultType="kr.co.ChimAcademy.vo.LectureVO">
		SELECT * 
		FROM lecture 	
		<where>
			<if test="depCode != null">
				depCode= #{depCode}
			</if>
			<if test="searchData != null">
				And (lecName LIKE CONCAT('%', #{searchData}, '%') OR lecCode = #{searchData})
			</if>
			<if test="lecClass != null">
				AND lecClass= #{lecClass}
			</if>
			<if test="majorCode != null">
				And majorCode = #{majorCode}
			</if>
			<if test="lecGubun != null">
				And lecGubun = #{lecGubun} ;
			</if>
		</where> 
			 
			 
			 
			 
			
	</select>




</mapper>